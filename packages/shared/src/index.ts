// Shared exports for types, constants, and helpers.
// Keep this file small at first and grow it as the project evolves.

export const PROJECT_NAME = 'Webhook-Driven Task Processing Pipeline';
export { logger } from './logger';
