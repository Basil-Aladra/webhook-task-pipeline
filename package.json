{
  "name": "webhook-driven-task-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for a webhook-driven task processing pipeline (API, worker, shared package).",
  "workspaces": [
    "apps/*",
    "apps/dashboard",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @webhook-pipeline/api",
    "dev:worker": "npm run dev -w @webhook-pipeline/worker",
    "build": "npm run build -w @webhook-pipeline/shared && npm run build -w @webhook-pipeline/api && npm run build -w @webhook-pipeline/worker && npm run build -w @webhook-pipeline/dashboard",
    "start:api": "npm run start -w @webhook-pipeline/api",
    "start:worker": "npm run start -w @webhook-pipeline/worker",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
